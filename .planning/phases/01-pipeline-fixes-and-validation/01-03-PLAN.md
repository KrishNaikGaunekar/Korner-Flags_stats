---
phase: 01-pipeline-fixes-and-validation
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified: []
autonomous: false
requirements: [PIPE-04]

must_haves:
  truths:
    - "Pipeline runs end-to-end on a soccer clip without crashing"
    - "Output is an H.264 MP4 file that exists on disk"
    - "Stats JSON file is generated alongside the output video"
    - "At least 80% of visible players are tracked with persistent IDs across frames (user visual confirmation)"
  artifacts:
    - path: "output_videos/*_annotated.mp4"
      provides: "Annotated output video"
    - path: "output_videos/*_stats.json"
      provides: "Stats JSON with possession and player data"
  key_links:
    - from: "main.py"
      to: "output_videos/"
      via: "Pipeline end-to-end execution"
      pattern: "save_video"
---

<objective>
Validate the full pipeline by running it on an available soccer clip and confirming YOLO detection quality (PIPE-04).

Purpose: Before committing to full NC State clip processing in Phase 2, confirm that all bug fixes from Plans 01 and 02 work correctly together and that YOLO detection tracks at least 80% of visible players with persistent IDs. This is the go/no-go gate for proceeding.

Output: Annotated MP4 video and stats JSON from a test run, plus user sign-off on detection quality.
</objective>

<execution_context>
@C:/Korner flag/.claude/get-shit-done/workflows/execute-plan.md
@C:/Korner flag/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-fixes-and-validation/1-CONTEXT.md
@.planning/phases/01-pipeline-fixes-and-validation/01-01-SUMMARY.md
@.planning/phases/01-pipeline-fixes-and-validation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pipeline on available soccer clip</name>
  <files></files>
  <read_first>main.py</read_first>
  <action>
1. Find an available soccer video clip on the machine. Check these locations in order:
   - Any .mp4, .avi, .mov, or .webm file in the project root or common subdirectories
   - `input_videos/` directory
   - `videos/` directory
   - `Stat tracker/` directory
   - If no video found, check for any video file on the machine using a targeted search

2. Run the pipeline on the found clip:
   ```bash
   python main.py --input <path_to_clip> --confidence 0.15
   ```

3. Verify the run completes without errors and produces:
   - An MP4 file in output_videos/ (not AVI)
   - A _stats.json file alongside it

4. Print the stats JSON content so the user can see possession and player data.

5. Confirm the output file is actually H.264 by checking with ffprobe:
   ```bash
   ffprobe -v error -select_streams v:0 -show_entries stream=codec_name -of csv=p=0 output_videos/*_annotated.mp4
   ```
   Expected output: `h264`

If no video clip is found anywhere, report this clearly so the user can provide one.
  </action>
  <verify>
    <automated>python -c "import os, glob; mp4s=glob.glob('output_videos/*_annotated.mp4'); jsons=glob.glob('output_videos/*_stats.json'); assert len(mp4s)>0, 'No annotated MP4 found'; assert len(jsons)>0, 'No stats JSON found'; print(f'Found: {mp4s[0]}, {jsons[0]}')"</automated>
  </verify>
  <acceptance_criteria>
- At least one file matching output_videos/*_annotated.mp4 exists
- At least one file matching output_videos/*_stats.json exists
- ffprobe reports codec_name as h264 for the output MP4
- Pipeline run completed without Python exceptions (exit code 0)
  </acceptance_criteria>
  <done>Pipeline ran end-to-end, produced H.264 MP4 output and stats JSON without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User validates YOLO detection quality</name>
  <files></files>
  <action>
CHECKPOINT: Present the output video and stats to the user for visual inspection.

What was built: The pipeline has been run on an available soccer clip with all bug fixes applied (camera movement correction flowing into view transformer, feature point refresh working, H.264 MP4 output). The annotated video and stats JSON are in output_videos/.

How to verify:
1. Open the annotated MP4 video in output_videos/ (play it in your browser or media player)
2. Check these detection quality criteria:
   - At least 80% of visible players have colored ellipses drawn around them
   - Player IDs remain consistent (same player keeps the same number across frames, no frequent ID switches)
   - Ball is tracked with a triangle marker for most of its visible frames
   - Team colors are reasonably assigned (two distinct colors for the two teams)
3. Check the stats JSON file:
   - Possession percentages are present and add up to ~100%
   - Player distances are nonzero (confirms speed/distance calculation is working with camera corrections)
4. Confirm the video plays in a browser (drag the MP4 into Chrome/Firefox/Edge — it should play without downloading a codec)

Resume signal: Type "approved" if detection quality is acceptable (80%+ players tracked). Otherwise describe issues seen (e.g., "only 50% of players detected", "IDs switch every few frames", "ball never tracked").
  </action>
  <verify>User confirms detection quality is acceptable</verify>
  <acceptance_criteria>
- User has watched the annotated output video
- User confirms at least 80% of visible players are tracked
- User confirms player IDs are reasonably persistent across frames
- User types "approved" or provides feedback for iteration
  </acceptance_criteria>
  <done>User has signed off on YOLO detection quality, confirming pipeline is ready for full NC State clip processing in Phase 2</done>
</task>

</tasks>

<verification>
- Pipeline runs without Python exceptions
- Output is H.264 MP4 (confirmed by ffprobe)
- Stats JSON contains non-empty possession and player data
- User confirms detection quality meets 80% threshold
</verification>

<success_criteria>
1. Pipeline completes end-to-end on a real soccer clip without errors
2. Output is browser-playable H.264 MP4 with faststart
3. Stats JSON generated with possession % and player speed/distance data
4. User confirms at least 80% of visible players are tracked with persistent IDs
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-fixes-and-validation/01-03-SUMMARY.md`
</output>
