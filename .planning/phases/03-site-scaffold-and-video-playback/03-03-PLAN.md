---
phase: 03-site-scaffold-and-video-playback
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: []
autonomous: false
requirements: [SITE-01, SITE-02, SITE-03]

must_haves:
  truths:
    - "GitHub Pages is enabled with source set to GitHub Actions"
    - "Pushing to main triggers a successful build and deploy"
    - "Visiting the live URL shows the clip gallery index page"
    - "Clicking a clip card navigates to a detail page with a working Plyr video player"
  artifacts: []
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "GitHub Pages environment"
      via: "withastro/action build + deploy-pages"
      pattern: "github-pages"
---

<objective>
Enable GitHub Pages on the repository, push the site to trigger deployment, and verify the live site works end-to-end.

Purpose: Complete SITE-01 (deployed site), and validate SITE-02 (gallery) and SITE-03 (video playback) on the live URL.
Output: A live, accessible GitHub Pages site at krishnaikgaunekar.github.io/Korner-Flags_stats/.
</objective>

<execution_context>
@C:/Korner flag/.claude/get-shit-done/workflows/execute-plan.md
@C:/Korner flag/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-site-scaffold-and-video-playback/03-CONTEXT.md
@.planning/phases/03-site-scaffold-and-video-playback/03-01-SUMMARY.md
@.planning/phases/03-site-scaffold-and-video-playback/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Enable GitHub Pages source to GitHub Actions</name>
  <files>N/A</files>
  <read_first>
    - .planning/phases/03-site-scaffold-and-video-playback/03-CONTEXT.md (GitHub Pages setup section)
    - .planning/phases/03-site-scaffold-and-video-playback/03-RESEARCH.md (Pitfall 5: Pages source not set)
  </read_first>
  <action>
HUMAN ACTION REQUIRED: The user must enable GitHub Pages in the repository settings. Claude cannot access repo settings via CLI.

Steps for the user:
1. Go to https://github.com/KrishNaikGaunekar/Korner-Flags_stats/settings/pages
2. Under "Build and deployment" > "Source", select "GitHub Actions" (not "Deploy from a branch")
3. Click Save
4. Confirm the source now shows "GitHub Actions"

Resume signal: Type "done" after completing the above steps.
  </action>
  <verify>
    <automated>echo "Manual verification — user confirms GitHub Pages source is set to GitHub Actions"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms GitHub Pages source is set to "GitHub Actions" in repo Settings > Pages
  </acceptance_criteria>
  <done>GitHub Pages source set to "GitHub Actions" in repository settings</done>
</task>

<task type="auto">
  <name>Task 2: Push to main and verify GitHub Actions deployment</name>
  <files>N/A</files>
  <read_first>
    - .github/workflows/deploy.yml (deployment workflow)
  </read_first>
  <action>
1. Ensure all Phase 3 files are committed and pushed to main:
   ```bash
   git add site/ .github/workflows/deploy.yml
   git status
   git push origin main
   ```

2. Wait for the GitHub Actions workflow to complete. Check status:
   ```bash
   gh run list --workflow=deploy.yml --limit=1
   ```

3. If the run is still in progress, wait and check again:
   ```bash
   gh run watch $(gh run list --workflow=deploy.yml --limit=1 --json databaseId --jq '.[0].databaseId')
   ```

4. Once the run completes, verify it succeeded:
   ```bash
   gh run view $(gh run list --workflow=deploy.yml --limit=1 --json databaseId --jq '.[0].databaseId')
   ```

5. If the run failed, read the logs to diagnose:
   ```bash
   gh run view $(gh run list --workflow=deploy.yml --limit=1 --json databaseId --jq '.[0].databaseId') --log-failed
   ```
   Common failures:
   - 403 on deploy: permissions block missing (should not happen — already in workflow)
   - Pages source not set to GitHub Actions: user must complete Task 1 first
   - Build error: check Astro build output in logs

6. Verify the live site responds:
   ```bash
   curl -I https://krishnaikgaunekar.github.io/Korner-Flags_stats/ 2>&1 | head -5
   ```
   Expected: HTTP 200 (may take 1-2 minutes after deploy completes for CDN propagation).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://krishnaikgaunekar.github.io/Korner-Flags_stats/ 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `gh run list --workflow=deploy.yml --limit=1` shows "completed" with "success" conclusion
    - `curl -s -o /dev/null -w "%{http_code}" https://krishnaikgaunekar.github.io/Korner-Flags_stats/` returns 200
  </acceptance_criteria>
  <done>GitHub Actions workflow deployed the site successfully and the live URL returns HTTP 200</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live site — gallery and video playback</name>
  <files>N/A</files>
  <read_first>
    - .planning/phases/03-site-scaffold-and-video-playback/03-CONTEXT.md (visual design decisions, what to verify)
    - .planning/REQUIREMENTS.md (SITE-01, SITE-02, SITE-03 definitions)
  </read_first>
  <action>
USER VISUAL VERIFICATION of the deployed site.

What was built:
- Index page: gallery grid with clip cards (thumbnail, title, duration, "View Analysis" link)
- Clip detail page: Plyr video player loading annotated MP4 from GitHub Releases CDN
- Apple-esque minimal design (light background, Inter font, subtle shadows)

Verification steps:
1. Visit https://krishnaikgaunekar.github.io/Korner-Flags_stats/
2. Verify the index page:
   - "Korner Flags" header with "AI-Powered Soccer Video Analysis" subtitle
   - At least one clip card visible with thumbnail image, title, duration, and "View Analysis" link
   - Clean, professional appearance (light background, card shadows, no broken images)
3. Click the "View Analysis" link on the clip card
4. Verify the clip detail page:
   - "Back to clips" link at top
   - Plyr video player visible with custom controls (play button, progress bar, volume, fullscreen)
   - Click Play — video loads and plays from GitHub Releases CDN
   - Placeholder sections visible below player: "Match Statistics" and "Team Heatmaps" with "Coming in the next update" text
5. Check browser DevTools Network tab — no 404 errors for thumbnails, CSS, or JS assets
6. Click "Back to clips" — returns to index page correctly

Resume signal: Type "approved" if the site looks correct and video plays, or describe any issues.
  </action>
  <verify>
    <automated>echo "Manual verification — user confirms gallery renders and video plays on live site"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms index page shows clip card gallery with working thumbnails
    - User confirms clip detail page has Plyr video player that plays the annotated MP4
    - User confirms no broken images or 404 errors in browser DevTools
    - User confirms navigation between index and detail pages works
  </acceptance_criteria>
  <done>User approved the live site — gallery and video playback verified on the deployed GitHub Pages URL</done>
</task>

</tasks>

<verification>
1. GitHub Pages source set to "GitHub Actions" in repo settings
2. GitHub Actions workflow completes with "success" status
3. Live URL returns HTTP 200
4. User visually confirms gallery and video playback work correctly
</verification>

<success_criteria>
- The site is live at krishnaikgaunekar.github.io/Korner-Flags_stats/
- Index page shows clip cards driven by manifest.json
- Clicking a card opens a detail page with a working Plyr video player
- All assets (thumbnails, CSS, JS) load without 404 errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-site-scaffold-and-video-playback/03-03-SUMMARY.md`
</output>
